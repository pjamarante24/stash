{
  "name": "@pamarante24/stash",
  "version": "1.1.0",
  "description": "simple caching module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "/lib"
  ],
  "scripts": {
    "test": "npm run build && jest",
    "build": "tsc --build",
    "benchmark": "npm run build && node test/default.benchmark.js && node test/onlyfs.benchmark.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "cache"
  ],
  "author": "Pablo Amarante",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.31",
    "jest": "^26.6.3",
    "tslint": "^6.1.3",
    "typescript": "^4.1.5"
  }
}
